---
interface Props {
	title: string;
	description?: string;
}

const { title, description } = Astro.props;
---

<div class="card" data-testid="card">
	<h2>{title}</h2>
	{description && <p>{description}</p>}
</div>
